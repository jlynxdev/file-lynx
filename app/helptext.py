class HelpText:
    groupby_date_format = 'Formatting to apply when grouping by last modification date.'
    groupby_by = 'Which field to group by ("ext" - extension, "date" - last modification date).'
    groupby_map = 'Maps extension names to subfolder names.'

    br_sep = 'Symbols that between filename and numerator.'
    br_num_start = 'Sets the starting point from which the counting starts.'
    br_num_first = 'Whether numerator should be placed at the beginning or at the end of the filename.'
    br_pad = 'The length of zero-padding applied to the numerator.'
    br_after = 'Filters out files with last modification datetime earlier than this one.'
    br_before = 'Filters out files with last modification datetime later than this one.'
    br_sort_by = 'Defines the value by which files will be sorted for numerating.'
    br_desc = 'Whether files should be sorted in descending order. Ignored if [--sort-by, -s] is None.'

    show_dir = 'The directory to show the contents of.'
    show_sort_by = 'Defines the value by which files will be sorted.'
    show_dirs = 'Show directories only.'
    show_files = 'Show files only.'
    show_desc = 'Display entries in descending order.'

    extensions = 'Comma-separated list of file extensions that will be taken into account (e.g. "txt,jpg,docx").'

    f_s_opt = 'Filtering & sorting options'
    num_sets = 'Numerator settings'

    show_version = 'Displays the current version of the program.'
